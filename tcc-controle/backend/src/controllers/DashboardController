const Movement = require("../models/Movement");

module.exports = {
    async show(req,res) {
        const { user_id } = req.headers;
        const ativos = "";

        const movements = await Movement.find({ user: user_id, responsibleDevolution: ativos });

        return res.json(movements);
    },
}